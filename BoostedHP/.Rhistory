library(BoostedHP)
data("IRE")
library(BoostedHP)
# raw HP filter
bx_HP = BoostedHP(IRE, lambda = lam, iter= FALSE)
lam = 100 # tuning parameter for the annaul data
# raw HP filter
bx_HP = BoostedHP(IRE, lambda = lam, iter= FALSE)
library(devtools)
document()
library(BoostedHP)
document()
